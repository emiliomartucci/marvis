# output/

Finished artifacts the project produces and hands out: reports, generated
documents, exports, deliverables.

Source material lives in `../input/`; durable write-ups of how something was
built go in `../docs/solutions/`.
