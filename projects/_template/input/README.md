# input/

Raw material you bring into the project: transcripts, exports, briefs, datasets,
reference documents. Things the work consumes but did not produce.

Keep originals here untouched; write derived artifacts to `../output/`.
